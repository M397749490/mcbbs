{
    "title": "求问插件编程部分代码不生效问题-高分悬赏",
    "author": "假面骑士TX",
    "replyCount": 0,
    "timestamp": 1464857400,
    "txt_content": "我试过用别的方法但是都一样，这是我的代码:\nLoad方法\n    public FileConfiguration load(String path){\n        if(path == \"\"){\n            path = \"config.yml\";\n        }\n        File file=new File(getDataFolder(),path);\n        if (!(file.exists())) {\n            try{\n                file.createNewFile();\n            }catch(IOException   e){\n                e.printStackTrace();\n            }\n        }\n        return YamlConfiguration.loadConfiguration(file);\n    }\nGUI界面(数据读取)方法\n        private void GUIs(String where,Player player) {\n        FileConfiguration configs = plugin.load(\"\");\n        if(where == null){\n            where = configs.getString(\"gui.main\");\n        }\n        int back = configs.getInt(\"gui.\"+where+\".back\",102);\n        String name = configs.getString(\"gui.\"+where+\".name\",\"Duang熔炉\");\n        int big = configs.getInt(\"gui.\"+where+\".big\",6);\n        if(big<1){big=1;}\n        if(big>6){big=6;}\n        Inventory inv = Bukkit.createInventory(player,big*9,name);\n        ItemStack backitem=new ItemStack(back); \n        backitem.getItemMeta().setDisplayName(\"§2§3§4§3§3§2§l\");\n        for(int i=0;i<=big*9-1;i++){\n            inv.setItem(i,backitem);\n        }\n        player.getPlayer().openInventory(inv);\n    }\n现在问题来了\nconfigs.getInt(路径,默认)永远只取默认getString同样是只取默认\n之前用的getconfig.getString也是这样只取默认\n将默认值删除使用就会报错\n求解！\n",
    "replies": []
}